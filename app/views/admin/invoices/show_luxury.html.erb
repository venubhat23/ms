<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Luxury Invoice #<%= @invoice.invoice_number %></title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800;900&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Inter', sans-serif;
        background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
        min-height: 100vh;
        padding: 30px;
        color: #e2e8f0;
      }

      .invoice-wrapper {
        max-width: 1000px;
        margin: 0 auto;
        background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
        border-radius: 24px;
        overflow: hidden;
        box-shadow:
          0 32px 64px rgba(0, 0, 0, 0.4),
          inset 0 1px 0 rgba(255, 255, 255, 0.1),
          0 0 0 1px rgba(255, 255, 255, 0.05);
        position: relative;
      }

      /* Luxury decorative elements */
      .invoice-wrapper::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 8px;
        background: linear-gradient(90deg,
          #ffd700 0%, #ffed4e 25%, #f59e0b 50%, #d97706 75%, #ffd700 100%);
        animation: shimmer 3s ease-in-out infinite;
      }

      .invoice-wrapper::after {
        content: '';
        position: absolute;
        top: -100px;
        right: -100px;
        width: 300px;
        height: 300px;
        background: radial-gradient(circle, rgba(255, 215, 0, 0.1) 0%, transparent 70%);
        border-radius: 50%;
        z-index: 1;
      }

      @keyframes shimmer {
        0%, 100% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
      }

      .invoice-content {
        position: relative;
        z-index: 2;
        padding: 50px;
      }

      /* Luxury Header */
      .luxury-header {
        display: grid;
        grid-template-columns: auto 1fr auto;
        gap: 40px;
        align-items: center;
        margin-bottom: 60px;
        border-bottom: 2px solid rgba(255, 215, 0, 0.3);
        padding-bottom: 40px;
      }

      .luxury-logo {
        width: 100px;
        height: 100px;
        background: linear-gradient(135deg, #ffd700, #f59e0b);
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 36px;
        color: #1a1a2e;
        font-weight: 900;
        box-shadow:
          0 20px 40px rgba(255, 215, 0, 0.3),
          inset 0 1px 0 rgba(255, 255, 255, 0.2);
        position: relative;
        overflow: hidden;
      }

      .luxury-logo-img {
        width: 90px;
        height: 90px;
        object-fit: contain;
        border-radius: 15px;
      }

      .luxury-logo::before {
        content: '';
        position: absolute;
        top: -2px;
        left: -2px;
        right: -2px;
        bottom: -2px;
        background: linear-gradient(135deg, #ffd700, #f59e0b);
        border-radius: 22px;
        z-index: -1;
        opacity: 0.5;
        animation: glow 2s ease-in-out infinite alternate;
      }

      @keyframes glow {
        from { box-shadow: 0 0 20px rgba(255, 215, 0, 0.3); }
        to { box-shadow: 0 0 40px rgba(255, 215, 0, 0.6); }
      }

      .company-details {
        text-align: center;
      }

      .company-name {
        font-family: 'Playfair Display', serif;
        font-size: 42px;
        font-weight: 900;
        background: linear-gradient(135deg, #ffd700, #f59e0b);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 16px;
        text-transform: uppercase;
        letter-spacing: 2px;
      }

      .company-tagline {
        font-size: 16px;
        color: #ffd700;
        font-weight: 300;
        font-style: italic;
        margin-bottom: 20px;
        opacity: 0.9;
      }

      .company-info {
        font-size: 14px;
        line-height: 1.8;
        color: #cbd5e1;
      }

      .company-info .highlight {
        color: #ffd700;
        font-weight: 600;
      }

      .invoice-title-section {
        text-align: right;
      }

      .invoice-title {
        font-family: 'Playfair Display', serif;
        font-size: 56px;
        font-weight: 900;
        background: linear-gradient(135deg, #e2e8f0, #94a3b8);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 16px;
        text-transform: uppercase;
        letter-spacing: 3px;
      }

      .invoice-subtitle {
        font-size: 18px;
        color: #64748b;
        font-weight: 300;
        margin-bottom: 24px;
      }

      /* Status and meta */
      .meta-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 24px;
        margin-bottom: 50px;
      }

      .meta-luxury-card {
        background: linear-gradient(135deg, rgba(255, 215, 0, 0.1), rgba(245, 158, 11, 0.05));
        border: 1px solid rgba(255, 215, 0, 0.2);
        border-radius: 16px;
        padding: 24px;
        text-align: center;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
      }

      .meta-luxury-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 16px 32px rgba(255, 215, 0, 0.2);
      }

      .meta-luxury-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #ffd700, #f59e0b);
      }

      .meta-label {
        font-size: 12px;
        font-weight: 700;
        color: #ffd700;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 8px;
      }

      .meta-value {
        font-size: 18px;
        font-weight: 700;
        color: #e2e8f0;
      }

      /* Bill Section */
      .bill-luxury-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        margin-bottom: 50px;
      }

      .bill-luxury-card {
        background: rgba(30, 41, 59, 0.8);
        border: 1px solid rgba(255, 215, 0, 0.1);
        border-radius: 20px;
        padding: 32px;
        position: relative;
        backdrop-filter: blur(10px);
      }

      .bill-luxury-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #ffd700, #f59e0b);
        border-radius: 20px 20px 0 0;
      }

      .bill-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 24px;
      }

      .bill-header h3 {
        font-size: 16px;
        font-weight: 700;
        color: #ffd700;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .bill-header i {
        font-size: 20px;
        color: #ffd700;
      }

      .client-name {
        font-size: 24px;
        font-weight: 700;
        color: #e2e8f0;
        margin-bottom: 12px;
        font-family: 'Playfair Display', serif;
      }

      .client-details {
        color: #94a3b8;
        font-size: 14px;
        line-height: 1.8;
      }

      /* Luxury Items Table */
      .items-luxury-section {
        margin-bottom: 50px;
      }

      .section-luxury-title {
        font-family: 'Playfair Display', serif;
        font-size: 28px;
        font-weight: 700;
        color: #e2e8f0;
        margin-bottom: 30px;
        display: flex;
        align-items: center;
        gap: 16px;
      }

      .section-luxury-title i {
        color: #ffd700;
        font-size: 32px;
      }

      .items-luxury-table {
        width: 100%;
        border-collapse: collapse;
        border-radius: 20px;
        overflow: hidden;
        background: rgba(30, 41, 59, 0.6);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 215, 0, 0.1);
      }

      .items-luxury-table thead {
        background: linear-gradient(135deg, #ffd700, #f59e0b);
        color: #1a1a2e;
      }

      .items-luxury-table th {
        padding: 24px 20px;
        font-size: 14px;
        font-weight: 700;
        text-align: left;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .items-luxury-table th:last-child {
        text-align: right;
      }

      .items-luxury-table td {
        padding: 24px 20px;
        font-size: 15px;
        border-bottom: 1px solid rgba(255, 215, 0, 0.1);
        vertical-align: top;
      }

      .items-luxury-table tbody tr {
        transition: all 0.3s ease;
      }

      .items-luxury-table tbody tr:hover {
        background: rgba(255, 215, 0, 0.05);
      }

      .item-luxury-description {
        font-weight: 600;
        color: #e2e8f0;
        margin-bottom: 6px;
        font-size: 16px;
      }

      .item-luxury-details {
        font-size: 13px;
        color: #94a3b8;
        font-style: italic;
      }

      .amount-luxury-cell {
        text-align: right;
        font-weight: 700;
        color: #ffd700;
        font-size: 18px;
      }

      /* Luxury Summary */
      .summary-luxury-section {
        display: grid;
        grid-template-columns: 1fr 450px;
        gap: 50px;
        margin-bottom: 50px;
      }

      .payment-luxury-info {
        background: rgba(30, 41, 59, 0.8);
        border: 1px solid rgba(255, 215, 0, 0.1);
        border-radius: 20px;
        padding: 32px;
        backdrop-filter: blur(10px);
      }

      .payment-luxury-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 28px;
      }

      .payment-luxury-header h3 {
        font-family: 'Playfair Display', serif;
        font-size: 22px;
        font-weight: 700;
        color: #e2e8f0;
      }

      .payment-luxury-header i {
        color: #ffd700;
        font-size: 24px;
      }

      .bank-luxury-details {
        display: grid;
        gap: 16px;
        margin-bottom: 32px;
      }

      .bank-luxury-detail {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid rgba(255, 215, 0, 0.1);
      }

      .bank-luxury-detail:last-child {
        border-bottom: none;
      }

      .bank-luxury-detail .label {
        font-weight: 500;
        color: #94a3b8;
      }

      .bank-luxury-detail .value {
        font-weight: 700;
        color: #e2e8f0;
      }

      .qr-luxury-section {
        text-align: center;
        padding: 24px;
        background: rgba(255, 215, 0, 0.05);
        border: 1px solid rgba(255, 215, 0, 0.2);
        border-radius: 16px;
        margin-top: 24px;
      }

      .qr-luxury-code {
        width: 140px;
        height: 140px;
        border-radius: 16px;
        margin: 0 auto 16px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
      }

      .total-luxury-summary {
        background: linear-gradient(135deg, #ffd700 0%, #f59e0b 100%);
        border-radius: 20px;
        padding: 32px;
        color: #1a1a2e;
        box-shadow:
          0 20px 40px rgba(255, 215, 0, 0.3),
          inset 0 1px 0 rgba(255, 255, 255, 0.2);
      }

      .summary-luxury-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
        padding-bottom: 20px;
        border-bottom: 1px solid rgba(26, 26, 46, 0.2);
        font-size: 16px;
      }

      .summary-luxury-row:last-child {
        margin-bottom: 0;
        padding-bottom: 0;
        border-bottom: none;
        font-size: 22px;
        font-weight: 900;
        border-top: 2px solid rgba(26, 26, 46, 0.3);
        padding-top: 20px;
      }

      .amount-luxury-words {
        background: rgba(26, 26, 46, 0.1);
        border-radius: 12px;
        padding: 20px;
        margin-top: 24px;
      }

      .amount-luxury-words h4 {
        font-size: 16px;
        margin-bottom: 12px;
        opacity: 0.8;
      }

      .amount-luxury-words .words {
        font-size: 18px;
        font-weight: 700;
        text-transform: capitalize;
      }

      /* Luxury Status Badge */
      .status-luxury-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 20px;
        border-radius: 25px;
        font-size: 14px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .status-luxury-paid {
        background: linear-gradient(135deg, #10b981, #059669);
        color: white;
        box-shadow: 0 8px 25px rgba(16, 185, 129, 0.3);
      }

      .status-luxury-pending {
        background: linear-gradient(135deg, #f59e0b, #d97706);
        color: white;
        box-shadow: 0 8px 25px rgba(245, 158, 11, 0.3);
      }

      .status-luxury-overdue {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
        box-shadow: 0 8px 25px rgba(239, 68, 68, 0.3);
      }

      /* Luxury Footer */
      .footer-luxury-section {
        display: grid;
        grid-template-columns: 1fr 300px;
        gap: 50px;
        margin-top: 60px;
        border-top: 2px solid rgba(255, 215, 0, 0.3);
        padding-top: 40px;
      }

      .terms-luxury-section {
        background: rgba(30, 41, 59, 0.6);
        border: 1px solid rgba(255, 215, 0, 0.1);
        border-radius: 20px;
        padding: 32px;
        backdrop-filter: blur(10px);
      }

      .terms-luxury-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 24px;
      }

      .terms-luxury-header h3 {
        font-family: 'Playfair Display', serif;
        font-size: 22px;
        font-weight: 700;
        color: #e2e8f0;
      }

      .terms-luxury-header i {
        color: #ffd700;
        font-size: 24px;
      }

      .terms-luxury-list {
        list-style: none;
        padding: 0;
      }

      .terms-luxury-list li {
        padding: 10px 0;
        font-size: 14px;
        color: #cbd5e1;
        position: relative;
        padding-left: 28px;
        line-height: 1.6;
      }

      .terms-luxury-list li::before {
        content: '◆';
        position: absolute;
        left: 0;
        color: #ffd700;
        font-weight: bold;
      }

      .signature-luxury-section {
        text-align: center;
      }

      .signature-luxury-box {
        height: 120px;
        border: 2px dashed rgba(255, 215, 0, 0.4);
        border-radius: 16px;
        margin-bottom: 20px;
        background: rgba(255, 215, 0, 0.02);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #64748b;
        font-style: italic;
        font-size: 14px;
      }

      .signature-luxury-label {
        font-size: 12px;
        font-weight: 700;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 8px;
      }

      .signature-luxury-name {
        font-family: 'Playfair Display', serif;
        font-size: 20px;
        font-weight: 700;
        color: #ffd700;
      }

      .signature-date {
        font-size: 12px;
        color: #64748b;
        margin-top: 12px;
      }

      /* Print Styles */
      @media print {
        body {
          background: white;
          color: black;
          padding: 0;
        }

        .invoice-wrapper {
          box-shadow: none;
          background: white;
          border-radius: 0;
        }

        .invoice-wrapper::before,
        .invoice-wrapper::after {
          display: none;
        }

        .luxury-logo::before {
          display: none;
        }
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .invoice-content {
          padding: 30px;
        }

        .luxury-header {
          grid-template-columns: 1fr;
          text-align: center;
        }

        .meta-grid {
          grid-template-columns: 1fr 1fr;
          gap: 16px;
        }

        .bill-luxury-section,
        .summary-luxury-section,
        .footer-luxury-section {
          grid-template-columns: 1fr;
        }

        .invoice-title {
          font-size: 36px;
        }

        .company-name {
          font-size: 28px;
        }
      }
    </style>
  </head>
  <body>
    <div class="invoice-wrapper">
      <div class="invoice-content">
        <!-- Luxury Header -->
        <header class="luxury-header">
          <div class="luxury-logo">
            <img src="/logo.jpeg" alt="Dhanvantri Insurance Services" class="luxury-logo-img" />
          </div>
          <div class="company-details">
            <h1 class="company-name">Dhanvantri Insurance Services</h1>
            <div class="company-tagline">Excellence in Every Policy • Trust in Every Service</div>
            <div class="company-info">
              <div><span class="highlight">Address:</span> Business Address Line 1, Business Address Line 2</div>
              <div>City, State, PIN Code</div>
              <div><span class="highlight">Mobile:</span> +91-9999999999 &nbsp;&nbsp; <span class="highlight">Email:</span> admin@dhanvantri.com</div>
              <div><span class="highlight">GSTIN:</span> 29AOIPT8113K1ZC &nbsp;&nbsp; <span class="highlight">PAN:</span> AOIPT8113K</div>
            </div>
          </div>
          <div class="invoice-title-section">
            <div class="invoice-title">Invoice</div>
            <div class="invoice-subtitle">Premium Service • Exceptional Quality</div>
            <div class="status-luxury-badge status-luxury-<%= @invoice.status %>">
              <% if @invoice.status == 'paid' %>
                <i class="fas fa-check-circle"></i>
              <% elsif @invoice.status == 'pending' %>
                <i class="fas fa-clock"></i>
              <% else %>
                <i class="fas fa-exclamation-triangle"></i>
              <% end %>
              <%= @invoice.status.humanize %>
            </div>
          </div>
        </header>

        <!-- Meta Information -->
        <div class="meta-grid">
          <div class="meta-luxury-card">
            <div class="meta-label">Invoice Number</div>
            <div class="meta-value">#<%= @invoice.invoice_number %></div>
          </div>
          <div class="meta-luxury-card">
            <div class="meta-label">Invoice Date</div>
            <div class="meta-value"><%= @invoice.invoice_date.strftime('%b %d, %Y') %></div>
          </div>
          <div class="meta-luxury-card">
            <div class="meta-label">Due Date</div>
            <div class="meta-value"><%= @invoice.due_date.strftime('%b %d, %Y') %></div>
          </div>
          <div class="meta-luxury-card">
            <div class="meta-label">Amount</div>
            <div class="meta-value">₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></div>
          </div>
        </div>

        <!-- Bill Section -->
        <div class="bill-luxury-section">
          <div class="bill-luxury-card">
            <div class="bill-header">
              <i class="fas fa-user-crown"></i>
              <h3>Bill To</h3>
            </div>
            <div class="client-name"><%= @invoice.recipient_name || @invoice.payout_recipient %></div>
            <div class="client-details">
              <div><%= @invoice.recipient_address || 'Address not provided' %></div>
              <% if @invoice.recipient_email.present? %>
                <div><i class="fas fa-envelope"></i> <%= @invoice.recipient_email %></div>
              <% end %>
              <% if @invoice.recipient_phone.present? %>
                <div><i class="fas fa-phone"></i> <%= @invoice.recipient_phone %></div>
              <% end %>
            </div>
          </div>
          <div class="bill-luxury-card">
            <div class="bill-header">
              <i class="fas fa-info-circle"></i>
              <h3>Service Details</h3>
            </div>
            <div class="client-name">Commission Payout - <%= @invoice.payout_type.humanize %></div>
            <div class="client-details">
              <div><strong>Service Type:</strong> Professional <%= @invoice.payout_type.humanize %> Commission</div>
              <div><strong>Payment Status:</strong> <%= @invoice.status.humanize %></div>
              <% if @invoice.paid_at.present? %>
                <div><strong>Payment Date:</strong> <%= @invoice.paid_at.strftime('%b %d, %Y at %I:%M %p') %></div>
              <% end %>
              <div><strong>Service Period:</strong> <%= @invoice.invoice_date.strftime('%B %Y') %></div>
            </div>
          </div>
        </div>

        <!-- Items Section -->
        <div class="items-luxury-section">
          <h2 class="section-luxury-title">
            <i class="fas fa-gem"></i>
            Premium Services Rendered
          </h2>
          <table class="items-luxury-table">
            <thead>
              <tr>
                <th>Service Description</th>
                <th>Category</th>
                <th>Quantity</th>
                <th>Rate</th>
                <th>Amount</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>
                  <div class="item-luxury-description">Premium Commission Payout - <%= @invoice.payout_type.humanize %></div>
                  <div class="item-luxury-details">Professional commission payment for exceptional insurance services rendered with premium quality and dedication</div>
                </td>
                <td><%= @invoice.payout_type.humanize %> Service</td>
                <td>1 Premium Payout</td>
                <td class="amount-luxury-cell">₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></td>
                <td class="amount-luxury-cell">₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></td>
              </tr>
            </tbody>
          </table>
        </div>

        <!-- Summary Section -->
        <div class="summary-luxury-section">
          <div class="payment-luxury-info">
            <div class="payment-luxury-header">
              <i class="fas fa-university"></i>
              <h3>Premium Banking Details</h3>
            </div>
            <div class="bank-luxury-details">
              <div class="bank-luxury-detail">
                <span class="label">Account Name:</span>
                <span class="value">Dhanvantri Insurance Services</span>
              </div>
              <div class="bank-luxury-detail">
                <span class="label">Premier Bank:</span>
                <span class="value">Canara Bank</span>
              </div>
              <div class="bank-luxury-detail">
                <span class="label">Account Number:</span>
                <span class="value">3194201000718</span>
              </div>
              <div class="bank-luxury-detail">
                <span class="label">IFSC Code:</span>
                <span class="value">CNRB0003194</span>
              </div>
              <div class="bank-luxury-detail">
                <span class="label">Account Type:</span>
                <span class="value">Premium Business Account</span>
              </div>
            </div>

            <div class="qr-luxury-section">
              <h4 style="margin-bottom: 16px; color: #e2e8f0; font-family: 'Playfair Display', serif;">Premium Payment Gateway</h4>
              <img src="https://api.qrserver.com/v1/create-qr-code/?size=140x140&data=upi://pay?pa=dhanvantri@ybl&pn=Dhanvantri%20Insurance%20Services&am=<%= @invoice.total_amount %>&cu=INR" alt="Premium Payment QR Code" class="qr-luxury-code" />
              <div style="font-size: 13px; color: #ffd700; margin-bottom: 12px; font-weight: 600;">UPI ID: dhanvantri@ybl</div>
              <div style="font-size: 11px; color: #94a3b8;">Scan for instant premium payment</div>
            </div>
          </div>

          <div class="total-luxury-summary">
            <div class="summary-luxury-row">
              <span>Service Subtotal:</span>
              <span>₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></span>
            </div>
            <div class="summary-luxury-row">
              <span>Premium Charges:</span>
              <span>₹0.00</span>
            </div>
            <div class="summary-luxury-row">
              <span>Taxes (N/A):</span>
              <span>₹0.00</span>
            </div>
            <div class="summary-luxury-row">
              <span>Total Premium Amount:</span>
              <span>₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></span>
            </div>

            <div class="amount-luxury-words">
              <h4>Amount in Words:</h4>
              <%
                total_amount_int = @invoice.total_amount.to_i
                ones = ["", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine"]
                teens = ["Ten", "Eleven", "Twelve", "Thirteen", "Fourteen", "Fifteen", "Sixteen", "Seventeen", "Eighteen", "Nineteen"]
                tens = ["", "", "Twenty", "Thirty", "Forty", "Fifty", "Sixty", "Seventy", "Eighty", "Ninety"]

                def convert_hundreds(num, ones, teens, tens)
                  result = ""
                  if num >= 100
                    result += ones[num / 100] + " Hundred "
                    num %= 100
                  end
                  if num >= 20
                    result += tens[num / 10] + " "
                    num %= 10
                  elsif num >= 10
                    result += teens[num - 10] + " "
                    num = 0
                  end
                  if num > 0
                    result += ones[num] + " "
                  end
                  result.strip
                end

                words = ""
                if total_amount_int >= 10000000
                  crores = total_amount_int / 10000000
                  words += convert_hundreds(crores, ones, teens, tens) + " Crore "
                  total_amount_int %= 10000000
                end
                if total_amount_int >= 100000
                  lakhs = total_amount_int / 100000
                  words += convert_hundreds(lakhs, ones, teens, tens) + " Lakh "
                  total_amount_int %= 100000
                end
                if total_amount_int >= 1000
                  thousands = total_amount_int / 1000
                  words += convert_hundreds(thousands, ones, teens, tens) + " Thousand "
                  total_amount_int %= 1000
                end
                if total_amount_int > 0
                  words += convert_hundreds(total_amount_int, ones, teens, tens)
                end
                words = words.strip
                words = "Zero" if words.empty?
              %>
              <div class="words"><%= words %> Rupees Only</div>
            </div>
          </div>
        </div>

        <!-- Terms and Signature -->
        <div class="footer-luxury-section">
          <div class="terms-luxury-section">
            <div class="terms-luxury-header">
              <i class="fas fa-scroll"></i>
              <h3>Premium Terms & Conditions</h3>
            </div>
            <% if @invoice.notes.present? %>
              <div style="color: #cbd5e1; font-size: 14px; line-height: 1.8;"><%= simple_format(@invoice.notes) %></div>
            <% else %>
              <ul class="terms-luxury-list">
                <li>This premium invoice represents commission payment for exceptional insurance services rendered with the highest standards of professionalism</li>
                <li>Payment terms are as per the premium service agreement between Dhanvantri Insurance Services and the esteemed recipient</li>
                <li>Any inquiries regarding this premium invoice should be addressed within 7 days of generation</li>
                <li>All payments are subject to applicable premium service charges, taxes and regulatory compliance</li>
                <li>This invoice is digitally authenticated and maintains the highest security standards</li>
                <li>Premium service guarantee: 100% satisfaction or full refund policy applies</li>
              </ul>
            <% end %>
          </div>

          <div class="signature-luxury-section">
            <div class="signature-luxury-box">
              Digital Premium Signature
            </div>
            <div class="signature-luxury-label">Authorized Premium Signatory</div>
            <div class="signature-luxury-name">Dhanvantri Insurance Services</div>
            <div class="signature-date">
              Premium Invoice Generated: <%= Date.current.strftime('%B %d, %Y') %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>