<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Premium Invoice #<%= @invoice.invoice_number %></title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
        line-height: 1.6;
      }

      .invoice-container {
        max-width: 900px;
        margin: 0 auto;
        background: white;
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        border-radius: 20px;
        overflow: hidden;
        position: relative;
      }

      /* PAID Watermark - Attractive Design */
      .paid-watermark {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%) rotate(-25deg);
        font-size: 150px;
        font-weight: 900;
        font-family: 'Arial Black', Arial, sans-serif;
        z-index: 10;
        pointer-events: none;

        /* Gradient text effect */
        background: linear-gradient(45deg, #28a745, #20c997);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;

        /* Fallback for browsers that don't support gradient text */
        color: rgba(40, 167, 69, 0.3);

        /* Multiple text shadows for depth */
        text-shadow:
          2px 2px 4px rgba(40, 167, 69, 0.2),
          4px 4px 8px rgba(40, 167, 69, 0.15),
          6px 6px 12px rgba(40, 167, 69, 0.1);

        /* Border effect */
        -webkit-text-stroke: 2px rgba(40, 167, 69, 0.1);

        letter-spacing: 15px;
        text-transform: uppercase;
      }

      /* Decorative elements */
      .invoice-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 6px;
        background: linear-gradient(90deg, #667eea, #764ba2, #f093fb);
      }

      .invoice-container::after {
        content: '';
        position: absolute;
        top: -50px;
        right: -50px;
        width: 200px;
        height: 200px;
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(244, 147, 251, 0.1));
        border-radius: 50%;
        z-index: 1;
      }

      .invoice-content {
        position: relative;
        z-index: 2;
        padding: 40px;
      }

      /* Header Section */
      .invoice-header {
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 40px;
        margin-bottom: 40px;
        align-items: center;
      }

      .company-info {
        display: flex;
        align-items: center;
        gap: 20px;
      }

      .company-logo-section {
        flex-shrink: 0;
      }

      .company-logo-premium {
        width: 80px;
        height: 80px;
        object-fit: contain;
        border-radius: 16px;
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        background: white;
        padding: 5px;
      }

      .company-logo {
        width: 80px;
        height: 80px;
        border-radius: 16px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 28px;
        font-weight: 700;
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
      }

      .company-details h1 {
        font-size: 32px;
        font-weight: 800;
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 8px;
      }

      .company-address {
        color: #64748b;
        font-size: 14px;
        line-height: 1.5;
      }

      .company-address .highlight {
        color: #334155;
        font-weight: 500;
      }

      .invoice-badge {
        text-align: right;
      }

      .invoice-title {
        font-size: 48px;
        font-weight: 900;
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 12px;
        text-transform: uppercase;
        letter-spacing: -2px;
      }

      .invoice-subtitle {
        font-size: 16px;
        color: #64748b;
        font-weight: 500;
        margin-bottom: 20px;
      }

      /* Invoice Meta Grid */
      .invoice-meta {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 40px;
      }

      .meta-card {
        background: linear-gradient(135deg, #f8fafc, #e2e8f0);
        border-radius: 16px;
        padding: 20px;
        text-align: center;
        border: 1px solid #e2e8f0;
        transition: all 0.3s ease;
      }

      .meta-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
      }

      .meta-card .label {
        font-size: 12px;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 8px;
      }

      .meta-card .value {
        font-size: 18px;
        font-weight: 700;
        color: #1e293b;
      }

      /* Bill To Section */
      .bill-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        margin-bottom: 40px;
      }

      .bill-card {
        background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
        border-radius: 16px;
        padding: 24px;
        border: 1px solid #e2e8f0;
        position: relative;
        overflow: hidden;
      }

      .bill-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #667eea, #764ba2);
      }

      .bill-card h3 {
        font-size: 14px;
        font-weight: 600;
        color: #667eea;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .bill-card h3 i {
        font-size: 16px;
      }

      .client-name {
        font-size: 20px;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 8px;
      }

      .client-details {
        color: #64748b;
        font-size: 14px;
        line-height: 1.6;
      }

      /* Items Table */
      .items-section {
        margin-bottom: 40px;
      }

      .section-title {
        font-size: 20px;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .section-title i {
        color: #667eea;
        font-size: 24px;
      }

      .items-table {
        width: 100%;
        border-collapse: collapse;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        border: 1px solid #e2e8f0;
      }

      .items-table thead {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
      }

      .items-table th {
        padding: 20px 16px;
        font-size: 14px;
        font-weight: 600;
        text-align: left;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .items-table th:last-child {
        text-align: right;
      }

      .items-table td {
        padding: 20px 16px;
        font-size: 14px;
        border-bottom: 1px solid #f1f5f9;
        vertical-align: top;
      }

      .items-table tbody tr {
        transition: background-color 0.2s ease;
      }

      .items-table tbody tr:hover {
        background-color: #f8fafc;
      }

      .items-table tbody tr:last-child td {
        border-bottom: none;
      }

      .item-description {
        font-weight: 600;
        color: #1e293b;
        margin-bottom: 4px;
      }

      .item-details {
        font-size: 12px;
        color: #64748b;
        font-style: italic;
      }

      .amount-cell {
        text-align: right;
        font-weight: 700;
        color: #1e293b;
        font-size: 16px;
      }

      /* Summary Section */
      .summary-section {
        display: grid;
        grid-template-columns: 1fr 400px;
        gap: 40px;
        margin-bottom: 40px;
      }

      .payment-info {
        background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
        border-radius: 16px;
        padding: 24px;
        border: 1px solid #e2e8f0;
      }

      .payment-info h3 {
        font-size: 18px;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .payment-info h3 i {
        color: #667eea;
      }

      .bank-details {
        display: grid;
        gap: 12px;
        margin-bottom: 24px;
      }

      .bank-detail {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid #f1f5f9;
      }

      .bank-detail:last-child {
        border-bottom: none;
      }

      .bank-detail .label {
        font-weight: 500;
        color: #64748b;
      }

      .bank-detail .value {
        font-weight: 600;
        color: #1e293b;
      }

      .qr-section {
        text-align: center;
        padding: 20px;
        background: #f8fafc;
        border-radius: 12px;
        margin-top: 20px;
      }

      .qr-code {
        width: 120px;
        height: 120px;
        border-radius: 12px;
        margin: 0 auto 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }

      .upi-badges {
        display: flex;
        justify-content: center;
        gap: 8px;
        flex-wrap: wrap;
        margin-top: 12px;
      }

      .upi-badge {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 11px;
        font-weight: 600;
        color: white;
      }

      .phonepe { background: #5f2d91; }
      .gpay { background: #4285f4; }
      .paytm { background: #00baf2; }
      .upi { background: #ff6600; }

      .total-summary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 16px;
        padding: 24px;
        color: white;
      }

      .summary-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 16px;
        padding-bottom: 16px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
      }

      .summary-row:last-child {
        margin-bottom: 0;
        padding-bottom: 0;
        border-bottom: none;
        font-size: 18px;
        font-weight: 700;
        border-top: 2px solid rgba(255, 255, 255, 0.3);
        padding-top: 16px;
      }

      .amount-words {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 16px;
        margin-top: 20px;
      }

      .amount-words h4 {
        font-size: 14px;
        margin-bottom: 8px;
        opacity: 0.9;
      }

      .amount-words .words {
        font-size: 16px;
        font-weight: 600;
        text-transform: capitalize;
      }

      /* Terms and Signature */
      .footer-section {
        display: grid;
        grid-template-columns: 1fr 250px;
        gap: 40px;
        margin-top: 40px;
      }

      .terms-section {
        background: #f8fafc;
        border-radius: 16px;
        padding: 24px;
        border: 1px solid #e2e8f0;
      }

      .terms-section h3 {
        font-size: 18px;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .terms-section h3 i {
        color: #667eea;
      }

      .terms-list {
        list-style: none;
        padding: 0;
      }

      .terms-list li {
        padding: 8px 0;
        font-size: 13px;
        color: #475569;
        position: relative;
        padding-left: 20px;
      }

      .terms-list li::before {
        content: '✓';
        position: absolute;
        left: 0;
        color: #667eea;
        font-weight: bold;
      }

      .signature-section {
        text-align: center;
      }

      .signature-box {
        height: 100px;
        border: 2px dashed #cbd5e1;
        border-radius: 12px;
        margin-bottom: 16px;
        background: #f8fafc;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #94a3b8;
        font-style: italic;
      }

      .signature-label {
        font-size: 12px;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 4px;
      }

      .signature-name {
        font-size: 16px;
        font-weight: 700;
        color: #667eea;
      }

      /* Status Badge */
      .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .status-paid {
        background: #dcfce7;
        color: #166534;
      }

      .status-pending {
        background: #fef3c7;
        color: #92400e;
      }

      .status-overdue {
        background: #fecaca;
        color: #991b1b;
      }

      /* Print Styles */
      @media print {
        body {
          background: white;
          padding: 0;
        }

        .invoice-container {
          box-shadow: none;
          border-radius: 0;
        }

        .invoice-content {
          padding: 20px;
        }

        .invoice-container::after {
          display: none;
        }
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .invoice-content {
          padding: 20px;
        }

        .invoice-header {
          grid-template-columns: 1fr;
          gap: 20px;
          text-align: center;
        }

        .invoice-meta {
          grid-template-columns: 1fr;
        }

        .bill-section {
          grid-template-columns: 1fr;
          gap: 20px;
        }

        .summary-section {
          grid-template-columns: 1fr;
          gap: 20px;
        }

        .footer-section {
          grid-template-columns: 1fr;
          gap: 20px;
        }

        .company-info {
          flex-direction: column;
          text-align: center;
        }

        .invoice-title {
          font-size: 32px;
        }
      }
    </style>
  </head>
  <body>
    <div class="invoice-container">
      <!-- PAID Watermark (only show when invoice is paid) -->
      <% if @invoice.status == 'paid' %>
        <div class="paid-watermark">PAID</div>
      <% end %>

      <div class="invoice-content">
        <!-- Header Section -->
        <header class="invoice-header">
          <div class="company-info">
            <div class="company-logo-section">
              <img src="/logo.jpeg" alt="Dhanvantri Insurance Services" class="company-logo-premium" />
            </div>
            <div class="company-details">
              <h1>Dhanvantri Insurance Services</h1>
              <div class="company-address">
                <div><strong class="highlight">Address:</strong> Business Address Line 1, Business Address Line 2</div>
                <div>City, State, PIN Code</div>
                <div><strong class="highlight">Mobile:</strong> +91-9999999999 &nbsp;&nbsp; <strong class="highlight">Email:</strong> admin@dhanvantri.com</div>
                <div><strong class="highlight">GSTIN:</strong> 29AOIPT8113K1ZC &nbsp;&nbsp; <strong class="highlight">PAN:</strong> AOIPT8113K</div>
              </div>
            </div>
          </div>
          <div class="invoice-badge">
            <div class="invoice-title">Invoice</div>
            <div class="invoice-subtitle">Premium Quality • Professional Service</div>
            <div class="status-badge status-<%= @invoice.status %>">
              <% if @invoice.status == 'paid' %>
                <i class="fas fa-check-circle"></i>
              <% elsif @invoice.status == 'pending' %>
                <i class="fas fa-clock"></i>
              <% else %>
                <i class="fas fa-exclamation-triangle"></i>
              <% end %>
              <%= @invoice.status.humanize %>
            </div>
          </div>
        </header>

        <!-- Invoice Meta Information -->
        <div class="invoice-meta">
          <div class="meta-card">
            <div class="label">Invoice Number</div>
            <div class="value">#<%= @invoice.invoice_number %></div>
          </div>
          <div class="meta-card">
            <div class="label">Invoice Date</div>
            <div class="value"><%= @invoice.invoice_date.strftime('%b %d, %Y') %></div>
          </div>
        </div>

        <!-- Bill To Section -->
        <div class="bill-section">
          <div class="bill-card">
            <h3><i class="fas fa-user"></i> Bill To</h3>
            <div class="client-name"><%= @invoice.recipient_name || @invoice.payout_recipient %></div>
            <div class="client-details">
              <div><%= @invoice.recipient_address || 'Address not provided' %></div>
              <% if @invoice.recipient_email.present? %>
                <div><i class="fas fa-envelope"></i> <%= @invoice.recipient_email %></div>
              <% end %>
              <% if @invoice.recipient_phone.present? %>
                <div><i class="fas fa-phone"></i> <%= @invoice.recipient_phone %></div>
              <% end %>
            </div>
          </div>
          <div class="bill-card">
            <h3><i class="fas fa-info-circle"></i> Payout Details</h3>
            <div class="client-name">Commission Payout - <%= @invoice.payout_type.humanize %></div>
            <div class="client-details">
              <div><strong>Payment Type:</strong> <%= @invoice.payout_type.humanize %> Commission</div>
              <div><strong>Status:</strong> <%= @invoice.status.humanize %></div>
              <% if @invoice.paid_at.present? %>
                <div><strong>Paid Date:</strong> <%= @invoice.paid_at.strftime('%b %d, %Y at %I:%M %p') %></div>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Items Section -->
        <div class="items-section">
          <h2 class="section-title">
            <i class="fas fa-list"></i>
            Service Details
          </h2>
          <table class="items-table">
            <thead>
              <tr>
                <th>Description</th>
                <th>Type</th>
                <th>Quantity</th>
                <th>Rate</th>
                <th>Amount</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>
                  <div class="item-description">Commission Payout - <%= @invoice.payout_type.humanize %></div>
                  <div class="item-details">Professional commission payment for insurance services rendered with excellence</div>
                </td>
                <td><%= @invoice.payout_type.humanize %></td>
                <td>1 Payout</td>
                <td class="amount-cell">₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></td>
                <td class="amount-cell">₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></td>
              </tr>
            </tbody>
          </table>
        </div>

        <!-- Summary Section -->
        <div class="summary-section">
          <div class="payment-info">
            <!-- Payment information removed as requested -->
          </div>

          <div class="total-summary">
            <div class="summary-row">
              <span>Subtotal:</span>
              <span>₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></span>
            </div>
            <div class="summary-row">
              <span>Tax (N/A):</span>
              <span>₹0.00</span>
            </div>
            <div class="summary-row">
              <span>Total Amount:</span>
              <span>₹<%= number_with_delimiter(@invoice.total_amount, delimiter: ',') %></span>
            </div>

            <div class="amount-words">
              <h4>Amount in Words:</h4>
              <%
                total_amount_int = @invoice.total_amount.to_i
                ones = ["", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine"]
                teens = ["Ten", "Eleven", "Twelve", "Thirteen", "Fourteen", "Fifteen", "Sixteen", "Seventeen", "Eighteen", "Nineteen"]
                tens = ["", "", "Twenty", "Thirty", "Forty", "Fifty", "Sixty", "Seventy", "Eighty", "Ninety"]

                def convert_hundreds(num, ones, teens, tens)
                  result = ""
                  if num >= 100
                    result += ones[num / 100] + " Hundred "
                    num %= 100
                  end
                  if num >= 20
                    result += tens[num / 10] + " "
                    num %= 10
                  elsif num >= 10
                    result += teens[num - 10] + " "
                    num = 0
                  end
                  if num > 0
                    result += ones[num] + " "
                  end
                  result.strip
                end

                words = ""
                if total_amount_int >= 10000000
                  crores = total_amount_int / 10000000
                  words += convert_hundreds(crores, ones, teens, tens) + " Crore "
                  total_amount_int %= 10000000
                end
                if total_amount_int >= 100000
                  lakhs = total_amount_int / 100000
                  words += convert_hundreds(lakhs, ones, teens, tens) + " Lakh "
                  total_amount_int %= 100000
                end
                if total_amount_int >= 1000
                  thousands = total_amount_int / 1000
                  words += convert_hundreds(thousands, ones, teens, tens) + " Thousand "
                  total_amount_int %= 1000
                end
                if total_amount_int > 0
                  words += convert_hundreds(total_amount_int, ones, teens, tens)
                end
                words = words.strip
                words = "Zero" if words.empty?
              %>
              <div class="words"><%= words %> Rupees Only</div>
            </div>
          </div>
        </div>

        <!-- Terms and Signature -->
        <div class="footer-section">
          <div class="terms-section">
            <h3><i class="fas fa-file-contract"></i> Terms & Conditions</h3>
            <% if @invoice.notes.present? %>
              <div style="color: #475569; font-size: 14px; line-height: 1.6;"><%= simple_format(@invoice.notes) %></div>
            <% else %>
              <ul class="terms-list">
                <li>This invoice represents commission payment for professional insurance services rendered</li>
                <li>Payment terms are as per the agreement between Dhanvantri Insurance Services and the recipient</li>
                <li>Any disputes regarding this invoice should be raised within 7 days of generation</li>
                <li>All payments are subject to applicable taxes and regulations</li>
                <li>This invoice is digitally generated and does not require a physical signature</li>
              </ul>
            <% end %>
          </div>

          <div class="signature-section">
            <div class="signature-box">
              Digital Signature
            </div>
            <div class="signature-label">Authorized Signatory</div>
            <div class="signature-name">Dhanvantri Insurance Services</div>
            <div style="font-size: 11px; color: #94a3b8; margin-top: 8px;">
              Generated on <%= Date.current.strftime('%b %d, %Y') %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>