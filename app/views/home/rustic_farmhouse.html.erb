<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Marali Santhe – Fresh Organic Produce & Groceries</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Caveat:wght@600;700&family=Nunito+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --wood:#6b4a34;--wood-dark:#4a3122;--terracotta:#c1652f;--terracotta-dark:#a3501f;
  --kraft:#e8dcc4;--cream:#fbf5e9;--dark:#3a2c1f;--olive:#7a8450;--muted:#8a7a63;
}
html{scroll-behavior:smooth}
body{font-family:'Nunito Sans',sans-serif;color:var(--dark);overflow-x:hidden;background:var(--cream)}
h1,h2,h3,h4{font-family:'DM Serif Display',serif;font-weight:400}
.accent{font-family:'Caveat',cursive;font-weight:700}

/* ── TOP BAR ── */
.top-bar{background:var(--wood-dark);color:var(--kraft);text-align:center;padding:10px 20px;font-size:12.5px;letter-spacing:.4px;font-weight:600}

/* ── NAV ── */
nav{display:flex;align-items:center;justify-content:space-between;padding:0 44px;height:86px;background:var(--cream);position:sticky;top:0;z-index:300;border-bottom:3px double var(--wood)}
.brand{display:flex;align-items:center;gap:14px;text-decoration:none}
.brand-mark{width:60px;height:60px;border-radius:50%;overflow:hidden;border:3px solid var(--terracotta);flex-shrink:0;box-shadow:0 4px 10px rgba(74,49,34,.2)}
.brand-mark img{width:100%;height:100%;object-fit:cover}
.brand-name{font-family:'DM Serif Display',serif;font-size:24px;color:var(--wood-dark)}
.nav-links{display:flex;gap:32px;list-style:none;align-items:center}
.nav-links a{text-decoration:none;color:var(--dark);font-size:13.5px;font-weight:700;letter-spacing:.3px}
.nav-links a:hover{color:var(--terracotta-dark)}
.nav-right{display:flex;align-items:center;gap:16px}
.cart-link{position:relative;font-size:21px;text-decoration:none;color:var(--wood-dark);display:flex;align-items:center}
.cart-badge{position:absolute;top:-7px;right:-11px;background:var(--terracotta);color:#fff;font-size:10px;font-weight:700;border-radius:50%;width:18px;height:18px;display:flex;align-items:center;justify-content:center}
.mobile-nav-links{display:none;position:absolute;top:100%;left:0;width:100%;background:var(--cream);border-bottom:3px double var(--wood);flex-direction:column;z-index:200}
.mobile-nav-links a{display:block;padding:16px 22px;text-decoration:none;color:var(--dark);font-weight:700;font-size:14px;border-bottom:1px dashed var(--muted)}
.mobile-menu-toggle{display:none;background:none;border:none;font-size:24px;color:var(--wood-dark);cursor:pointer}

/* ── BUTTONS ── */
.btn{display:inline-flex;align-items:center;gap:8px;background:var(--terracotta);color:#fff;padding:15px 34px;text-decoration:none;font-size:13.5px;font-weight:700;border:2px solid var(--wood-dark);border-radius:6px;cursor:pointer;font-family:'Nunito Sans',sans-serif;transition:transform .18s;box-shadow:3px 3px 0 var(--wood-dark)}
.btn:hover{transform:translate(-2px,-2px);box-shadow:5px 5px 0 var(--wood-dark)}
.btn-olive{display:inline-flex;align-items:center;gap:8px;background:var(--olive);color:#fff;padding:15px 34px;text-decoration:none;font-size:13.5px;font-weight:700;border:2px solid var(--wood-dark);border-radius:6px;box-shadow:3px 3px 0 var(--wood-dark);transition:transform .18s}
.btn-olive:hover{transform:translate(-2px,-2px);box-shadow:5px 5px 0 var(--wood-dark)}
.btn-outline{padding:13px 32px;border:2px solid var(--wood-dark);background:transparent;color:var(--wood-dark);font-weight:700;font-size:13.5px;border-radius:6px;cursor:pointer;font-family:'Nunito Sans',sans-serif;transition:all .2s;text-decoration:none;display:inline-block}
.btn-outline:hover{background:var(--wood-dark);color:#fff}
.btn-outline.white{border-color:#fff;color:#fff}
.btn-outline.white:hover{background:#fff;color:var(--wood-dark)}

/* ── HERO ── */
.hero{position:relative;min-height:88vh;display:flex;align-items:center;background:var(--kraft);overflow:hidden;border-bottom:3px double var(--wood)}
.hero-body{position:relative;z-index:2;display:flex;align-items:center;width:100%;max-width:1320px;margin:0 auto;padding:0 56px;gap:60px}
.hero-left{flex:1}
.hero-eyebrow{display:inline-block;background:var(--wood-dark);color:var(--kraft);font-size:11.5px;font-weight:700;padding:8px 20px;border-radius:4px;margin-bottom:22px;letter-spacing:1px;text-transform:uppercase}
.hero h1{font-size:clamp(38px,5.2vw,64px);line-height:1.15;color:var(--wood-dark);margin-bottom:8px}
.hero .accent{font-size:clamp(46px,6vw,80px);color:var(--terracotta-dark);display:block;line-height:1;margin-bottom:24px}
.hero-sub{font-size:15px;color:var(--muted);line-height:1.85;max-width:440px;margin-bottom:34px}
.hero-ctas{display:flex;gap:16px;flex-wrap:wrap}
.hero-right{flex:1;position:relative;display:flex;justify-content:center}
.hero-right img{width:100%;max-width:480px;border-radius:6px;box-shadow:10px 10px 0 var(--wood),0 20px 50px rgba(74,49,34,.25);object-fit:cover;height:460px;border:4px solid #fff}
.hero-tag{position:absolute;bottom:-18px;left:8%;background:var(--terracotta);color:#fff;padding:12px 22px;border-radius:6px;font-size:12px;font-weight:700;box-shadow:0 8px 20px rgba(0,0,0,.2);transform:rotate(-3deg)}

/* ── TRUST ── */
.trust{background:var(--wood-dark);display:flex;justify-content:center;flex-wrap:wrap}
.trust-item{flex:1;min-width:180px;display:flex;align-items:center;justify-content:center;gap:10px;padding:22px 20px;color:var(--kraft);border-right:1px dashed rgba(255,255,255,.2)}
.trust-item:last-child{border:none}
.trust-item .ti{font-size:22px}
.trust-item p{font-size:12px;font-weight:700}

/* ── SECTION LABELS ── */
.sec-title{text-align:center;padding:70px 20px 6px;font-size:clamp(28px,3.4vw,44px);color:var(--wood-dark)}
.sec-title .accent{display:inline;font-size:1.15em;color:var(--terracotta-dark)}
.sec-sub{text-align:center;font-size:14px;color:var(--muted);padding:0 20px 40px}

/* ── CATEGORY CARDS ── */
.cats{display:grid;grid-template-columns:1fr 1fr;gap:24px;max-width:1280px;margin:0 auto 20px;padding:0 40px}
.cat{position:relative;overflow:hidden;border-radius:8px;cursor:pointer;border:3px solid #fff;box-shadow:0 14px 34px rgba(74,49,34,.18)}
.cat img{width:100%;height:400px;object-fit:cover;display:block;transition:transform .6s;filter:sepia(.08)}
.cat:hover img{transform:scale(1.06)}
.cat-shade{position:absolute;inset:0;background:linear-gradient(to top,rgba(58,44,31,.78) 0%,transparent 55%)}
.cat-info{position:absolute;bottom:0;left:0;right:0;padding:32px}
.cat-badge{display:inline-block;background:var(--terracotta);color:#fff;font-size:11px;font-weight:700;padding:6px 16px;border-radius:4px;margin-bottom:12px}
.cat-info h3{font-size:30px;color:#fff;margin-bottom:16px}

/* ── PRODUCT GRID ── */
.pgrid{display:grid;grid-template-columns:repeat(4,1fr);gap:22px;max-width:1280px;margin:0 auto 72px;padding:0 40px}
.pcard{background:#fff;border-radius:8px;overflow:hidden;border:2px solid var(--kraft);box-shadow:4px 4px 0 var(--kraft);transition:transform .2s,box-shadow .2s;cursor:pointer}
.pcard:hover{transform:translate(-3px,-3px);box-shadow:7px 7px 0 var(--kraft)}
.pcard-img{position:relative;height:210px;overflow:hidden;background:var(--kraft)}
.pcard-img img{width:100%;height:100%;object-fit:cover;transition:transform .5s;filter:sepia(.06)}
.pcard:hover .pcard-img img{transform:scale(1.08)}
.ptag{position:absolute;top:12px;left:12px;font-size:10px;font-weight:700;padding:5px 12px;border-radius:4px;color:#fff}
.ptag.organic{background:var(--olive)}
.ptag.new{background:var(--terracotta)}
.ptag.super{background:#8e5a3a}
.ptag.oos{background:#999}
.ptag.best{background:var(--wood)}
.pcard-body{padding:18px 18px 22px}
.pcard h4{font-family:'DM Serif Display',serif;font-size:18px;margin-bottom:4px}
.pcard .psub{font-size:11.5px;color:var(--muted);margin-bottom:12px}
.pcard .pprice{font-size:17px;font-weight:800;margin-bottom:14px;color:var(--terracotta-dark)}
.pcard .pprice s{font-weight:400;color:#ccc;font-size:12px;margin-right:5px}
.btn-add{width:100%;padding:11px;background:var(--wood-dark);color:#fff;border:none;cursor:pointer;font-family:'Nunito Sans',sans-serif;font-size:11.5px;font-weight:700;border-radius:4px;transition:background .2s}
.btn-add:hover{background:var(--terracotta)}
.btn-add.out{background:#e2ddd3;color:#aaa;cursor:not-allowed}

/* ── BANNER ── */
.banner{position:relative;min-height:520px;overflow:hidden;display:flex;align-items:center;justify-content:center}
.banner-bg-img{position:absolute;inset:0;background:url('https://images.pexels.com/photos/1640774/pexels-photo-1640774.jpeg?auto=compress&cs=tinysrgb&w=1600') center/cover;filter:sepia(.2)}
.banner::before{content:'';position:absolute;inset:0;background:rgba(58,44,31,.68)}
.banner-body{position:relative;z-index:2;text-align:center;color:#fff;max-width:720px;padding:60px 24px}
.banner-body .leaf{font-size:44px;display:block;margin-bottom:14px}
.banner-body h2{font-size:clamp(28px,4.2vw,50px);margin-bottom:18px;color:#fff}
.banner-body p{font-size:14.5px;line-height:1.9;color:rgba(255,255,255,.85);margin-bottom:12px}

/* ── WHY STRIP ── */
.why-strip{background:var(--kraft);display:grid;grid-template-columns:repeat(4,1fr);gap:2px;border-top:3px double var(--wood);border-bottom:3px double var(--wood)}
.why-item{display:flex;flex-direction:column;align-items:center;justify-content:center;padding:44px 22px;text-align:center;border-right:1px dashed var(--muted)}
.why-item:last-child{border:none}
.why-icon{font-size:36px;margin-bottom:14px}
.why-item h4{font-size:19px;color:var(--wood-dark);margin-bottom:6px}
.why-item p{font-size:12px;color:var(--muted);font-weight:600}

/* ── SPLIT ── */
.split{display:flex;min-height:520px}
.split-img{flex:1;overflow:hidden;position:relative}
.split-img img{width:100%;height:100%;object-fit:cover;display:block;filter:sepia(.08);transition:transform .6s}
.split:hover .split-img img{transform:scale(1.04)}
.split-text{flex:1;background:var(--cream);display:flex;align-items:center;padding:60px 68px}
.split-quote{font-family:'Caveat',cursive;font-weight:700;font-size:30px;color:var(--terracotta-dark);display:block;margin:12px 0 20px}
.split-text h2{font-size:clamp(24px,3vw,40px);margin-bottom:12px;color:var(--wood-dark)}
.split-text p{font-size:14px;line-height:1.9;color:var(--muted);margin-bottom:12px}
.split-rev{flex-direction:row-reverse}
.split-rev .split-text{background:var(--wood-dark)}
.split-rev .split-text h2{color:#fff}
.split-rev .split-text p{color:rgba(255,255,255,.65)}
.split-rev .split-quote{color:var(--kraft)}

/* ── VIDEO ── */
.vid-section{display:flex;min-height:500px}
.vid-left{flex:1;overflow:hidden}
.vid-left iframe{width:100%;height:100%;min-height:500px;display:block;border:none}
.vid-right{flex:1;background:var(--olive);display:flex;align-items:center;padding:60px 56px;color:#fff}
.vid-right h2{font-size:32px;margin-bottom:16px;color:#fff}
.vid-right p{font-size:14px;line-height:1.9;color:rgba(255,255,255,.85);margin-bottom:12px}

/* ── REVIEWS ── */
.reviews{background:var(--kraft);padding:80px 28px}
.reviews-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;max-width:1120px;margin:0 auto}
.rcard{background:#fff;padding:34px 28px;border-radius:8px;text-align:center;border:2px solid #fff;box-shadow:5px 5px 0 rgba(74,49,34,.12);transition:transform .2s}
.rcard:hover{transform:translate(-3px,-3px)}
.stars{color:var(--terracotta);font-size:19px;letter-spacing:3px;margin-bottom:14px}
.rcard blockquote{font-size:14px;line-height:1.85;color:#5b4c3d;margin-bottom:16px;font-style:italic}
.rcard cite{font-weight:800;font-style:normal;font-size:13px;color:var(--wood-dark)}

/* ── ARRIVALS ── */
.arrivals{display:grid;grid-template-columns:1fr 1fr;max-width:1280px;margin:0 auto 72px;gap:20px;padding:0 40px}
.acard{position:relative;overflow:hidden;border-radius:8px;cursor:pointer;border:3px solid #fff;box-shadow:0 14px 30px rgba(74,49,34,.16)}
.acard img{width:100%;height:440px;object-fit:cover;display:block;transition:transform .6s;filter:sepia(.06)}
.acard:hover img{transform:scale(1.06)}
.acard-overlay{position:absolute;bottom:0;left:0;right:0;background:linear-gradient(transparent,rgba(58,44,31,.8));padding:52px 30px 30px;color:#fff}
.acard-overlay .atag{display:inline-block;background:var(--terracotta);color:#fff;font-size:10.5px;font-weight:700;padding:6px 16px;border-radius:4px;margin-bottom:12px}
.acard-overlay h3{font-size:28px;color:#fff}

/* ── GALLERY ── */
.gallery-section{padding:60px 40px 0}
.gallery-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;max-width:1280px;margin:0 auto}
.gcard{overflow:hidden;border-radius:8px;cursor:pointer;position:relative;border:3px solid #fff;box-shadow:0 6px 16px rgba(74,49,34,.14)}
.gcard img{width:100%;height:230px;object-fit:cover;display:block;transition:transform .5s;filter:sepia(.08)}
.gcard:hover img{transform:scale(1.1)}

/* ── FEATURED ── */
.featured{background:var(--cream);padding:88px 40px}
.feat-wrap{max-width:1100px;margin:0 auto;display:grid;grid-template-columns:1fr 1fr;gap:70px;align-items:center}
.feat-img-box{position:relative}
.feat-img-box img{width:100%;border-radius:8px;box-shadow:8px 8px 0 var(--kraft);object-fit:cover;max-height:460px;border:4px solid #fff}
.feat-badge{position:absolute;top:-16px;right:-16px;background:var(--terracotta);color:#fff;width:84px;height:84px;border-radius:50%;display:flex;flex-direction:column;align-items:center;justify-content:center;font-size:10.5px;font-weight:700;text-align:center;line-height:1.4;transform:rotate(8deg);border:3px solid #fff}
.feat-info h2{font-size:36px;margin-bottom:10px;color:var(--wood-dark)}
.feat-price{font-size:28px;font-weight:800;color:var(--terracotta-dark);margin:14px 0}
.feat-info>p{font-size:14px;line-height:1.85;color:var(--muted);margin-bottom:8px}
.feat-pills{display:flex;gap:10px;flex-wrap:wrap;margin:18px 0}
.pill{background:var(--kraft);color:var(--wood-dark);font-size:11.5px;font-weight:700;padding:7px 16px;border-radius:4px}

/* ── BLOG ── */
.blog-section{padding:80px 28px;background:var(--kraft)}
.blog-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:26px;max-width:1200px;margin:0 auto}
.bcard{border-radius:8px;overflow:hidden;border:2px solid #fff;box-shadow:5px 5px 0 rgba(74,49,34,.1);cursor:pointer;transition:transform .2s;background:#fff}
.bcard:hover{transform:translate(-3px,-3px)}
.bcard img{width:100%;height:200px;object-fit:cover;display:block;filter:sepia(.06)}
.bcard-body{padding:22px}
.bcard-body .bdate{font-size:11px;color:var(--terracotta-dark);font-weight:700;margin-bottom:8px}
.bcard-body h4{font-family:'DM Serif Display',serif;font-size:20px;margin-bottom:8px;color:var(--wood-dark)}
.bcard-body p{font-size:12.5px;color:var(--muted);line-height:1.7}

/* ── FOOTER ── */
footer{background:var(--wood-dark);color:rgba(255,255,255,.65);padding:64px 44px 30px;border-top:6px solid var(--terracotta)}
.footer-grid{display:grid;grid-template-columns:2fr 1fr 1fr 1fr;gap:44px;margin-bottom:48px}
.fbrand-name{font-family:'DM Serif Display',serif;font-size:28px;color:var(--kraft);margin-bottom:12px}
.fbrand p{font-size:13px;line-height:1.9;max-width:280px}
.fcol h5{color:#fff;font-size:12px;letter-spacing:1px;text-transform:uppercase;font-weight:700;margin-bottom:18px}
.fcol ul{list-style:none}
.fcol ul li{margin-bottom:10px}
.fcol ul li a{text-decoration:none;color:rgba(255,255,255,.6);font-size:13px}
.fcol ul li a:hover{color:var(--kraft)}
.footer-contact{font-size:13px;line-height:1.85;margin-top:14px}
.footer-contact b{color:#fff}
.footer-bottom{border-top:1px dashed rgba(255,255,255,.2);padding-top:24px;display:flex;justify-content:space-between;align-items:center;font-size:12px;color:rgba(255,255,255,.4);flex-wrap:wrap;gap:12px}
.footer-policy-links{display:flex;gap:20px}
.footer-policy-links a{color:rgba(255,255,255,.55);text-decoration:none;font-size:12px}
.footer-policy-links a:hover{color:var(--kraft)}

.toast{position:fixed;bottom:28px;left:50%;transform:translateX(-50%) translateY(80px);background:var(--wood-dark);color:#fff;padding:14px 30px;border-radius:6px;font-size:13px;font-weight:700;z-index:9999;opacity:0;transition:opacity .3s,transform .3s;pointer-events:none}
.toast.show{opacity:1;transform:translateX(-50%) translateY(0)}

@media(max-width:960px){
  nav{padding:0 18px}
  .mobile-menu-toggle{display:block}
  .nav-links{display:none}
  .mobile-nav-links.mobile-open{display:flex}
  .hero-body{flex-direction:column;padding:44px 24px;text-align:center}
  .hero-right{margin-top:36px}
  .cats,.pgrid,.reviews-grid,.arrivals,.blog-grid,.feat-wrap,.footer-grid{grid-template-columns:1fr 1fr}
  .gallery-grid{grid-template-columns:1fr 1fr}
  .split,.split-rev,.vid-section{flex-direction:column}
  .split-img,.vid-left{min-height:280px}
  .why-strip{grid-template-columns:1fr 1fr}
}
@media(max-width:600px){
  .cats,.pgrid,.reviews-grid,.arrivals,.blog-grid,.footer-grid,.gallery-grid,.why-strip{grid-template-columns:1fr}
  .split-text{padding:40px 26px}
  .featured,.blog-section,.reviews{padding:56px 18px}
  footer{padding:44px 20px 24px}
}
</style>
</head>
<body>

<div class="toast" id="toast">🧺 Added to basket!</div>

<div class="top-bar">
  🚜 Free delivery within 5km of JP Nagar (₹499+) · Free across Bengaluru (₹3000+)
</div>

<nav>
  <a class="brand" href="/">
    <div class="brand-mark"><img src="/logo.jpeg" alt="Marali Santhe"></div>
    <span class="brand-name">Marali Santhe</span>
  </a>
  <ul class="nav-links">
    <li><a href="#">Home</a></li>
    <li><a href="#">Organic Foods</a></li>
    <li><a href="#">Wellness</a></li>
    <li><a href="#">Combos</a></li>
    <li><a href="#">Blogs</a></li>
  </ul>
  <div class="nav-right">
    <a href="<%= track_order_path %>" class="track-order-link" aria-label="Track Order" title="Track Order" style="display:inline-flex;align-items:center;gap:5px;padding:7px 14px;border:1.5px solid currentColor;border-radius:20px;font-size:12px;font-weight:700;text-decoration:none;color:inherit;white-space:nowrap;">📦 Track</a>
    <a href="<%= storefront_cart_path %>" class="cart-link" aria-label="Cart">
      🛍️<% if cart_count > 0 %><span class="cart-badge"><%= cart_count %></span><% end %>
    </a>
    <a href="/customer/login" class="btn-outline" style="padding:10px 22px;font-size:13px">Login</a>
    <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">☰</button>
  </div>
  <div class="mobile-nav-links" id="mobileNav">
    <a href="#">Home</a><a href="#">Organic Foods</a><a href="#">Wellness</a><a href="#">Combos</a><a href="#">Blogs</a><a href="<%= track_order_path %>">📦 Track Order</a><a href="<%= storefront_cart_path %>">Cart (<%= cart_count %>)</a><a href="/customer/login">Login</a>
  </div>
</nav>

<section class="hero">
  <div class="hero-body">
    <div class="hero-left">
      <span class="hero-eyebrow">🌾 Certified Organic Farmstead</span>
      <h1>Straight from the farmhouse to</h1>
      <span class="accent">your table</span>
      <p class="hero-sub">Marali Santhe brings honest, chemical-free produce from small family farms — grown with care, harvested by hand, delivered fresh.</p>
      <div class="hero-ctas">
        <a href="#" class="btn">Shop the Harvest</a>
        <a href="#" class="btn-outline">Meet Our Farmers</a>
      </div>
    </div>
    <div class="hero-right">
      <img src="https://images.pexels.com/photos/1300972/pexels-photo-1300972.jpeg?auto=compress&cs=tinysrgb&w=1200" alt="Marali Santhe organic produce">
      <div class="hero-tag">Fresh off the vine 🌿</div>
    </div>
  </div>
</section>

<div class="trust">
  <div class="trust-item"><span class="ti">🌱</span><p>100% Organic</p></div>
  <div class="trust-item"><span class="ti">🚚</span><p>Free Delivery ₹499+</p></div>
  <div class="trust-item"><span class="ti">🔄</span><p>7-Day Returns</p></div>
  <div class="trust-item"><span class="ti">🏆</span><p>Award Winning</p></div>
  <div class="trust-item"><span class="ti">🇮🇳</span><p>Made in India</p></div>
</div>

<div class="cats">
  <% if @top_category %>
    <div class="cat">
      <img src="<%= @top_category.display_image_url %>" alt="<%= @top_category.name %>">
      <div class="cat-shade"></div>
      <div class="cat-info"><div class="cat-badge">★ Best Seller</div><h3><%= @top_category.name %></h3><a href="#" class="btn-outline white">Shop Now</a></div>
    </div>
  <% end %>
  <% if @secondary_category %>
    <div class="cat">
      <img src="<%= @secondary_category.display_image_url %>" alt="<%= @secondary_category.name %>">
      <div class="cat-shade"></div>
      <div class="cat-info"><div class="cat-badge">Also Popular</div><h3><%= @secondary_category.name %></h3><a href="#" class="btn-outline white">Shop Now</a></div>
    </div>
  <% end %>
</div>

<h2 class="sec-title">Trending <span class="accent">This Week</span></h2>
<p class="sec-sub">
  <% if @products_by_category.present? %>
    <%= pluralize(@total_products, 'item') %> across <%= pluralize(@products_by_category.size, 'category') %> — fresh, pure and full of goodness
  <% else %>
    Our catalog is being restocked — please check back shortly
  <% end %>
</p>

<% @products_by_category.each do |category_name, products| %>
  <h3 style="text-align:center;font-size:20px;margin:0 0 20px;color:var(--terracotta-dark)"><%= category_name %> <span style="color:var(--muted);font-size:13px;font-weight:600;font-family:'Nunito Sans',sans-serif"><%= pluralize(products.size, 'item') %></span></h3>
  <div class="pgrid">
    <% products.each do |product| %>
      <div class="pcard">
        <div class="pcard-img">
          <% if product.main_image_url %>
            <%= image_tag product.main_image_url, alt: product.name %>
          <% else %>
            <span style="display:flex;align-items:center;justify-content:center;height:100%;font-size:32px;opacity:.35">🌾</span>
          <% end %>
          <% if !product.in_stock? %>
            <span class="ptag oos">Out of Stock</span>
          <% elsif @best_seller_ids.include?(product.id) %>
            <span class="ptag best">★ Best Seller</span>
          <% elsif @new_arrival_ids.include?(product.id) %>
            <span class="ptag super">🆕 New Arrival</span>
          <% elsif product.discounted? %>
            <span class="ptag new">Sale</span>
          <% end %>
        </div>
        <div class="pcard-body">
          <h4><%= product.name %></h4>
          <p class="psub"><%= [product.unit_type, product.sku].compact_blank.join(' · ') %></p>
          <div class="pprice">
            <% if product.discounted? %>
              <s>₹<%= product.price %></s> ₹<%= product.selling_price %>
            <% else %>
              ₹<%= product.selling_price %>
            <% end %>
          </div>
          <% if product.in_stock? %>
            <button class="btn-add" onclick="addToCart(<%= product.id %>, '<%= j product.name %>', this)">Add to Basket</button>
          <% else %>
            <button class="btn-add out" disabled>Out of Stock</button>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>

<div class="banner">
  <div class="banner-bg-img"></div>
  <div class="banner-body">
    <span class="leaf">🌿</span>
    <h2>What makes our products special?</h2>
    <p>Marali Santhe sources the finest produce directly from certified organic farms across India — free from pesticides, chemicals and artificial additives.</p>
    <a href="#" class="btn-olive" style="margin-top:16px;display:inline-block">Explore All Products</a>
  </div>
</div>

<div class="why-strip">
  <div class="why-item"><span class="why-icon">🌾</span><h4>Premium Blend</h4><p>Finest Organic Farms</p></div>
  <div class="why-item"><span class="why-icon">🍃</span><h4>100% Natural</h4><p>No Chemicals · No Additives</p></div>
  <div class="why-item"><span class="why-icon">👐</span><h4>Hand Picked</h4><p>Carefully Sourced Daily</p></div>
  <div class="why-item"><span class="why-icon">📦</span><h4>Freshly Packed</h4><p>Sealed For Max Freshness</p></div>
</div>

<div class="split">
  <div class="split-img"><img src="https://images.pexels.com/photos/2252584/pexels-photo-2252584.jpeg?auto=compress&cs=tinysrgb&w=900" alt="Farm"></div>
  <div class="split-text">
    <div>
      <p style="font-size:12px;text-transform:uppercase;color:var(--olive);font-weight:700;margin-bottom:10px">🚜 Our Farms</p>
      <h2>From Our Farms,<br>To Your Table</h2>
      <span class="split-quote">"Pure. Natural. Fresh."</span>
      <p>We work directly with certified organic farmers across India — no middlemen, no compromises. Every ingredient is traceable back to its source.</p>
      <a href="#" class="btn-outline" style="margin-top:12px;display:inline-block">Our Story</a>
    </div>
  </div>
</div>

<div class="split split-rev">
  <div class="split-img"><img src="https://images.pexels.com/photos/1640777/pexels-photo-1640777.jpeg?auto=compress&cs=tinysrgb&w=900" alt="Organic Process"></div>
  <div class="split-text">
    <div>
      <p style="font-size:12px;text-transform:uppercase;color:var(--kraft);font-weight:700;margin-bottom:10px">🍯 Our Process</p>
      <h2>Art of Creating the Perfect Organic</h2>
      <span class="split-quote">"Quality You Can Taste."</span>
      <p style="color:rgba(255,255,255,.65)">Our experts blend and process every product to give you unmatched purity with delicious taste in every serving.</p>
      <a href="#" class="btn" style="margin-top:12px;display:inline-block">Shop Now</a>
    </div>
  </div>
</div>

<div class="vid-section">
  <div class="vid-left"><iframe src="https://www.youtube.com/embed/hFZFjoX2cGg?rel=0&modestbranding=1&controls=1" title="Organic Living" allow="accelerometer;autoplay;clipboard-write;encrypted-media;gyroscope;picture-in-picture" allowfullscreen></iframe></div>
  <div class="vid-right">
    <div>
      <p style="font-size:12px;text-transform:uppercase;font-weight:700;margin-bottom:14px;opacity:.9">🎬 Watch &amp; Learn</p>
      <h2>The Art of Living Organically</h2>
      <p>Our farmers and wellness experts work together to create the finest organic products — from soil to shelf, zero compromise.</p>
      <a href="#" class="btn-outline white" style="margin-top:8px">Watch Our Story</a>
    </div>
  </div>
</div>

<div class="reviews">
  <h2 class="sec-title" style="padding-top:0">What Our Customers Say</h2>
  <p class="sec-sub">Loved by families across India</p>
  <div class="reviews-grid">
    <div class="rcard"><div class="stars">★★★★★</div><blockquote>"The Wild Honey is absolutely pure — you can taste the difference instantly. My whole family is hooked!"</blockquote><cite>— Priya Sharma</cite></div>
    <div class="rcard"><div class="stars">★★★★★</div><blockquote>"Best organic atta I've ever used. The rotis come out so soft and flavourful."</blockquote><cite>— Rahul Mehta</cite></div>
    <div class="rcard"><div class="stars">★★★★★</div><blockquote>"Moringa powder is a game changer. Genuine quality, delivery was super fast!"</blockquote><cite>— Ananya Nair</cite></div>
  </div>
</div>

<h2 class="sec-title">New <span class="accent">Arrivals</span></h2>
<p class="sec-sub">Fresh additions to the Marali Santhe family</p>
<div class="arrivals">
  <% @new_arrivals.first(2).each do |product| %>
    <div class="acard">
      <% if product.main_image_url %><%= image_tag product.main_image_url, alt: product.name %><% end %>
      <div class="acard-overlay"><div class="atag">New Arrival</div><h3><%= product.name %></h3></div>
    </div>
  <% end %>
</div>

<div class="gallery-section">
  <h2 class="sec-title" style="padding-top:0">Gallery</h2>
  <p class="sec-sub">A glimpse into the world of Marali Santhe</p>
  <div class="gallery-grid">
    <div class="gcard"><img src="/logo1.jpeg" alt="Marali Santhe"></div>
    <div class="gcard"><img src="https://images.pexels.com/photos/1638280/pexels-photo-1638280.jpeg?auto=compress&cs=tinysrgb&w=600" alt=""></div>
    <div class="gcard"><img src="https://images.pexels.com/photos/2252584/pexels-photo-2252584.jpeg?auto=compress&cs=tinysrgb&w=600" alt=""></div>
    <div class="gcard"><img src="https://images.pexels.com/photos/1640774/pexels-photo-1640774.jpeg?auto=compress&cs=tinysrgb&w=600" alt=""></div>
  </div>
</div>

<div class="featured">
  <h2 class="sec-title" style="padding-top:0">Featured Product</h2>
  <div class="feat-wrap">
    <div class="feat-img-box"><img src="https://images.pexels.com/photos/1638280/pexels-photo-1638280.jpeg?auto=compress&cs=tinysrgb&w=700" alt="Wild Forest Honey"><div class="feat-badge">100%<br>Organic</div></div>
    <div class="feat-info">
      <h2>Wild Forest Honey</h2>
      <p>Sourced from pristine forest regions of India, this raw honey is cold-extracted, unheated and unfiltered to preserve all natural enzymes and antioxidants.</p>
      <div class="feat-pills"><span class="pill">🌿 Organic</span><span class="pill">🍯 Raw</span><span class="pill">🧪 Lab Tested</span></div>
      <div class="feat-price">₹420</div>
      <a href="#" class="btn">Add to Basket</a>
    </div>
  </div>
</div>

<div class="blog-section">
  <h2 class="sec-title" style="padding-top:0">From the Blog</h2>
  <p class="sec-sub">Tips, recipes &amp; organic living insights</p>
  <div class="blog-grid">
    <div class="bcard"><img src="https://images.pexels.com/photos/1640774/pexels-photo-1640774.jpeg?auto=compress&cs=tinysrgb&w=600" alt="Blog"><div class="bcard-body"><p class="bdate">January 30, 2025</p><h4>5 Benefits of Switching to Organic Food</h4><p>Discover how organic food can transform your health.</p></div></div>
    <div class="bcard"><img src="https://images.pexels.com/photos/1300975/pexels-photo-1300975.jpeg?auto=compress&cs=tinysrgb&w=600" alt="Blog"><div class="bcard-body"><p class="bdate">January 31, 2025</p><h4>How to Identify Genuine Organic Products</h4><p>Learn the key certifications to look for.</p></div></div>
    <div class="bcard"><img src="https://images.pexels.com/photos/2802527/pexels-photo-2802527.jpeg?auto=compress&cs=tinysrgb&w=600" alt="Blog"><div class="bcard-body"><p class="bdate">February 5, 2025</p><h4>The Magic of Turmeric</h4><p>Explore the incredible health benefits of turmeric.</p></div></div>
  </div>
</div>

<footer>
  <div class="footer-grid">
    <div class="fbrand">
      <div class="fbrand-name">Marali Santhe</div>
      <p>Bringing the goodness of certified organic farms directly to your table. Pure, natural and full of freshness.</p>
      <div class="footer-contact">
        <b>Marali Santhe</b><br>Owner: Raghunandan<br><br>
        #64, Ground Floor, 7th Cross, 24th Main,<br>RK Colony, JP Nagar 2nd Phase, Bengaluru - 560078<br><br>
        Contact: 9035408833<br>Email: maralisanthe@gmail.com<br>GSTIN: 29AWEPR9293C1Z0
      </div>
    </div>
    <div class="fcol"><h5>Shop</h5><ul><li><a href="#">Organic Foods</a></li><li><a href="#">Superfoods</a></li><li><a href="#">Spices &amp; Herbs</a></li><li><a href="#">Combo Packs</a></li><li><a href="#">New Arrivals</a></li></ul></div>
    <div class="fcol"><h5>Customer Support</h5><ul><li><a href="#">FAQs</a></li><li><a href="#">About Us</a></li><li><a href="#">Our Farms</a></li><li><a href="#">Contact Us</a></li><li><a href="#">Blogs</a></li></ul></div>
  </div>
  <div class="footer-bottom">
    <span>© 2025 Marali Santhe. All rights reserved.</span>
    <div class="footer-policy-links"><a href="/terms-and-conditions.html" target="_blank">Terms &amp; Conditions</a><a href="/privacy-policy.html" target="_blank">Privacy Policy</a></div>
  </div>
</footer>

<script>
function toggleMobileMenu(){document.getElementById('mobileNav').classList.toggle('mobile-open')}

async function addToCart(productId, name, btn) {
  const orig = btn.textContent;
  btn.disabled = true;
  btn.textContent = 'Adding…';
  try {
    const res = await fetch('<%= add_item_storefront_cart_path %>', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ product_id: productId, quantity: 1 })
    });
    const data = await res.json();
    if (!data.success) {
      alert(data.error || 'Could not add item to cart.');
      btn.disabled = false;
      btn.textContent = orig;
      return;
    }
    const t = document.getElementById('toast');
    t.textContent = '🧺 ' + (name || 'Item') + ' added to basket!';
    t.classList.add('show');
    setTimeout(() => t.classList.remove('show'), 2200);
    btn.textContent = 'Added ✓';
    setTimeout(() => { btn.disabled = false; btn.textContent = orig; }, 1600);

    const badge = document.querySelector('.cart-badge');
    if (badge) {
      badge.textContent = data.cart_count;
    } else if (data.cart_count > 0) {
      const cartLink = document.querySelector('.cart-link');
      const span = document.createElement('span');
      span.className = 'cart-badge';
      span.textContent = data.cart_count;
      cartLink.appendChild(span);
    }
  } catch (e) {
    alert('Could not add item to cart.');
    btn.disabled = false;
    btn.textContent = orig;
  }
}
</script>
</body>
</html>
