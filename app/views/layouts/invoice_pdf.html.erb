<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - Marali Santhe</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -webkit-system-font, sans-serif;
            background: white;
            line-height: 1.6;
            color: #2d3748;
            font-size: 14px;
        }

        .invoice-container {
            width: 100%;
            background: white;
            overflow: hidden;
        }

        /* Header with gradient background */
        .invoice-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            position: relative;
        }

        .header-content {
            position: relative;
        }

        .farm-logo {
            width: 70px;
            height: 70px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 15px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .farm-logo i {
            font-size: 35px;
            color: white;
        }

        .company-name {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 6px;
            letter-spacing: -0.5px;
        }

        .company-tagline {
            font-size: 14px;
            opacity: 0.9;
            margin-bottom: 20px;
            font-weight: 400;
        }

        .company-details {
            font-size: 13px;
            line-height: 1.6;
            opacity: 0.95;
        }

        .company-details i {
            width: 18px;
            text-align: center;
            margin-right: 8px;
        }

        /* Invoice title section */
        .invoice-title-section {
            padding: 25px 30px;
            background: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
        }

        .invoice-title {
            font-size: 24px;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 8px;
        }

        .invoice-meta {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 15px;
        }

        .invoice-meta .meta-group h6 {
            font-weight: 600;
            color: #4a5568;
            margin-bottom: 4px;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .invoice-meta .meta-group p {
            font-size: 14px;
            color: #2d3748;
            margin: 0;
            font-weight: 500;
        }

        /* Customer section */
        .customer-section {
            padding: 25px 30px;
            background: white;
        }

        .section-title {
            font-size: 16px;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
        }

        .section-title i {
            margin-right: 8px;
            color: #667eea;
        }

        .customer-details {
            background: #f7fafc;
            padding: 18px;
            border-radius: 8px;
            border-left: 3px solid #667eea;
        }

        /* Items table */
        .items-section {
            padding: 0 30px 25px;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            background: white;
            border-radius: 8px;
            overflow: hidden;
            border: 1px solid #e2e8f0;
        }

        .items-table thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .items-table thead th {
            padding: 12px;
            color: white;
            font-weight: 600;
            text-align: left;
            font-size: 12px;
            border: none;
        }

        .items-table tbody tr {
            border-bottom: 1px solid #e2e8f0;
        }

        .items-table tbody tr:last-child {
            border-bottom: none;
        }

        .items-table td {
            padding: 12px;
            border: none;
            font-size: 13px;
            color: #4a5568;
            vertical-align: top;
        }

        .item-name {
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 3px;
        }

        .item-description {
            font-size: 11px;
            color: #718096;
        }

        .text-right {
            text-align: right;
        }

        .text-center {
            text-align: center;
        }

        /* Totals section */
        .totals-section {
            padding: 25px 30px;
            background: #f8f9fa;
            border-top: 1px solid #e2e8f0;
        }

        .totals-container {
            max-width: 350px;
            margin-left: auto;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 6px 0;
            font-size: 13px;
        }

        .total-row.subtotal {
            color: #4a5568;
        }

        .total-row.tax {
            color: #4a5568;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 12px;
            margin-bottom: 12px;
        }

        .total-row.final-total {
            font-size: 18px;
            font-weight: 700;
            color: #2d3748;
            border: 2px solid #667eea;
            border-radius: 6px;
            padding: 12px 15px;
            background: white;
        }

        /* Footer */
        .invoice-footer {
            padding: 25px 30px;
            background: white;
            text-align: center;
            border-top: 1px solid #e2e8f0;
        }

        .footer-note {
            font-size: 13px;
            color: #718096;
            margin-bottom: 12px;
        }

        .footer-contact {
            font-size: 11px;
            color: #a0aec0;
        }

        /* A4 page setup */
        @page {
            size: A4 portrait;
            margin: 10mm 12mm;
        }

        /* Page break utilities */
        .page-break-before {
            page-break-before: always;
        }

        .page-break-after {
            page-break-after: always;
        }

        .page-break-avoid {
            page-break-inside: avoid;
        }

        /* Prevent widows and orphans */
        p, h1, h2, h3, h4, h5, h6 {
            orphans: 3;
            widows: 3;
        }

        /* Company letterhead watermark */
        .watermark {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 72px;
            color: rgba(102, 126, 234, 0.05);
            font-weight: 800;
            z-index: -1;
            pointer-events: none;
        }
    </style>
</head>
<body>
    <!-- Watermark -->
    <div class="watermark">DEMOFARM</div>

    <div class="invoice-container">
        <%= yield %>
    </div>
</body>
</html>