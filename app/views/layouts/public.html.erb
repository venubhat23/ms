<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= content_for?(:title) ? yield(:title) : "ADHIKA - Dhanvantri Insurance" %></title>

    <!-- SEO Meta Tags -->
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'ADHIKA by Dhanvantri Insurance - Your trusted insurance partner' %>">
    <meta name="keywords" content="insurance, adhika, dhanvantri, policy, privacy">
    <meta name="author" content="Dhanvantri Insurance">

    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'ADHIKA - Dhanvantri Insurance' %>">
    <meta property="og:description" content="<%= content_for?(:description) ? yield(:description) : 'ADHIKA by Dhanvantri Insurance - Your trusted insurance partner' %>">
    <meta property="og:type" content="website">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Stylesheets -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%= javascript_importmap_tags %>

    <style>
      body {
        font-family: 'Inter', sans-serif;
        line-height: 1.6;
        color: #333;
      }

      .hero-section {
        background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
        color: white;
        padding: 4rem 0 2rem;
      }

      .brand-logo {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
      }

      .content-section {
        padding: 3rem 0;
      }

      .section-card {
        background: white;
        border-radius: 12px;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        border: 1px solid #f0f0f0;
      }

      .section-title {
        color: var(--color-primary);
        font-weight: 600;
        margin-bottom: 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1rem;
        margin-top: 1rem;
      }

      .info-item {
        background: #f8f9ff;
        padding: 1rem;
        border-radius: 8px;
        border-left: 4px solid var(--color-primary);
      }

      .contact-card {
        background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
        color: white;
        text-align: center;
      }

      .contact-info {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        margin: 0.5rem 0;
      }

      .footer-section {
        background: #2d3748;
        color: white;
        padding: 2rem 0;
        text-align: center;
      }

      /* Additional responsive styles */
      @media (max-width: 768px) {
        .hero-section {
          padding: 2rem 0 1rem;
        }

        .brand-logo {
          font-size: 1.5rem;
        }

        .display-5 {
          font-size: 1.8rem !important;
        }

        .info-grid {
          grid-template-columns: 1fr;
        }

        .contact-info {
          flex-direction: column;
          gap: 0.25rem;
        }
      }

      /* Smooth scroll and animations */
      html {
        scroll-behavior: smooth;
      }

      .section-card {
        transform: translateY(0);
        transition: all 0.3s ease;
      }

      .section-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
      }

      /* Loading animation */
      .fade-in {
        opacity: 0;
        animation: fadeIn 0.6s ease-in-out forwards;
      }

      @keyframes fadeIn {
        to {
          opacity: 1;
        }
      }
    </style>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark" style="background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);">
      <div class="container">
        <a class="navbar-brand fw-bold fs-4" href="#">
          <i class="bi bi-shield-check me-2"></i>
          ADHIKA
        </a>
      </div>
    </nav>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="footer-section">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <p class="mb-2">&copy; 2025 Dhanvantri Insurance. All rights reserved.</p>
            <p class="mb-0 text-muted">ADHIKA - Your trusted insurance companion</p>
          </div>
        </div>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Custom JavaScript -->
    <script>
      // Add fade-in animation to cards when they come into view
      document.addEventListener('DOMContentLoaded', function() {
        const cards = document.querySelectorAll('.section-card');

        const observer = new IntersectionObserver((entries) => {
          entries.forEach((entry, index) => {
            if (entry.isIntersecting) {
              setTimeout(() => {
                entry.target.classList.add('fade-in');
              }, index * 100);
            }
          });
        }, {
          threshold: 0.1
        });

        cards.forEach(card => {
          observer.observe(card);
        });

        // Smooth scroll for any internal links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          anchor.addEventListener('click', function (e) {
            e.preventDefault();
            document.querySelector(this.getAttribute('href')).scrollIntoView({
              behavior: 'smooth'
            });
          });
        });

        // Add a subtle glow effect to contact buttons
        const contactBtn = document.querySelector('.contact-card .btn');
        if (contactBtn) {
          contactBtn.addEventListener('mouseenter', function() {
            this.style.transform = 'scale(1.05)';
            this.style.transition = 'all 0.3s ease';
          });
          contactBtn.addEventListener('mouseleave', function() {
            this.style.transform = 'scale(1)';
          });
        }
      });
    </script>
  </body>
</html>